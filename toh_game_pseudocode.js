class TowersOfHanoi {
   run() {
     //prompt user which disk to move from and to
     // check if fromPile has disk, store value
     // check that fromPile value is less than top disk of toPile
     // if both are valid, move disk
     // check if game is won
     // if not, prompt user again
   }
 }
